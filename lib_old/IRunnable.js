/**
 * The 'runnable' interface is used to define something that can be
 * run during the run step of the bootstrap process.
 **/
var Runnable = {
    /**
     * A method used to configure instances of this injectable.
     **/
    $run: function() {}
};

module.exports = Runnable;