/**
 * The core app service creates an express app and allows the developer to 
 * inject a $app variable that can be used to access this app in a service.
 **/
var acute = require('../index');

/**
 * Define the module for the app
 **/
exports = new acute.module('core', [function() {
    
}])
    